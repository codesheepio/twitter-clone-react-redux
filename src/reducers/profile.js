const initialState = {
  name: 'N/A',
  screenName: 'notavailable',
  numTweets: 0,
  numFollowers: 0,
  numFollowings: 0,
}

const profileReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default profileReducer
